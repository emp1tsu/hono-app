{
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "start-firebase-emulator": "firebase emulators:start --project hono-app-3513b",
    "wrangler": "wrangler"
  },
  "dependencies": {
    "@hono/firebase-auth": "^1.3.4",
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "firebase-tools": "^13.0.3",
    "wrangler": "^3.22.0"
  }
}
